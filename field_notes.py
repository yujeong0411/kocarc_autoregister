# -*- coding: utf-8 -*-
"""
항목별 도움말(셀 메모) 내용 — 여기만 채우면 됩니다.

■ 사용법
  NOTES = { "필드명": "메모 내용", ... }

  · 필드명 = 각 시트 2행(회색 줄)에 적힌 이름.
      - 합쳐진 칸이면 2행이 마커로 보임(@DT:/@VUK:/@TREE:/@GROUP:).
        마커 뒤 이름 아무거나 쓰면 됩니다.
        예) 2행이  @VUK:CKMB,CKMB_YN,ND   →  "CKMB"
            2행이  @DT:Y_DEATH_DATE,...    →  "Y_DEATH_DATE"
  · 메모 내용 = 여러 줄이면  \n  으로 줄바꿈.
  · 저장한 뒤 build_template.py 를 다시 실행하면 헤더 셀(빨간 삼각형)에 메모가 붙습니다.
  · 봇은 셀 '값'만 읽으므로 메모는 크롤링에 전혀 영향 없음.
    (엑셀에서 손으로 달면 재생성 때 파일이 덮여써져 사라지니 반드시 여기에 적으세요.)
  · 없는 필드명을 쓰면 build 로그에 "⚠ 메모 미매칭 필드명 …" 으로 알려줍니다.

아래 3개는 형식 예시(지침서 원문 기반) — 지우거나 고쳐서 쓰세요.
"""

NOTES = {
    "PAT_NM": "환자 이름의 영문 이니셜.\n⚠ 알 수 없으면 '999' 로 입력.",
    "ER_DATE": "등록병원 응급실에 내원한 시각.\n⚠ 응급실 '등록'시각이 실제 내원시각보다 늦은 경우, 실제 내원시각으로 기록.",
    "Y_HEIGHT": "심정지 환자의 등록병원 내원시 신장(cm).\n누운 상태에서 측정. 측정할 수 없으면 추정값이라도 입력.",
}
